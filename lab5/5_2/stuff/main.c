// C implementation of digital clock
#include <stdio.h>
#include <time.h>

// driver code
int main(int argc, char **argv) {
    for (int i = 0; i < argc; ++i){
        printf("%s\n", argv[i]);
    }
	return 0;
}
